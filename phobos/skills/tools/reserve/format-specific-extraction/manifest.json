{
  "id": "format-specific-extraction",
  "name": "format specific extraction",
  "description": "``` ZIP archive → Security validation → XML parsing → Text + tables + metadata",
  "version": "1.0.0",
  "scope": "both",
  "category": "tools",
  "trigger": "Invoke when the task matches this skill.",
  "runner": null
}
